#!/usr/bin/env bash
set -euo pipefail
if [[ "${1:-}" == "ps" ]]; then
  exec docker compose \
    --project-name cashtrack \
    -f /opt/cashtrack/compose.obs.yml \
    ps --orphans=false "${@:2}"
else
  exec docker compose \
    --project-name cashtrack \
    -f /opt/cashtrack/compose.obs.yml \
    "$@"
fi
